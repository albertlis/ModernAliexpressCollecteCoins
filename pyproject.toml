[project]
name = "AliexpressCollecteCoins"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright",
    "python-dotenv",
    "playwright-stealth",
    "pyautogui",
    "schedule",
]
